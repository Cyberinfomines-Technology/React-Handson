import React from 'react'

const Navbar = () => {
  return (
    <div className='navbar'>
      <button>Home</button>
      <button>About us</button>
      <button>Login</button>
    </div>
  )
}

export default Navbar;
